name: OpenCode PR Review
on:
  pull_request:
    types: [opened]
permissions:
  pull-requests: write
  contents: read
jobs:
  review:
    uses: nebarilabs/.github/.github/workflows/opencode-review.yml@v2
