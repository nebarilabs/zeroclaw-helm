name: OpenCode PR Review
on:
  pull_request:
    types: [opened, synchronize]
permissions:
  pull-requests: write
  contents: read
jobs:
  review:
    uses: nebarilabs/.github/.github/workflows/opencode-review.yml@v1
