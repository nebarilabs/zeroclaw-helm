# Centrally managed by nebarilabs/.github — do not edit in this repo.
# The org sync (nebarilabs/.github .github/workflows/sync.yml) overwrites this
# file directly; downstream edits are silently lost. Propose changes upstream.
name: OKF Lint
on:
  pull_request:
    paths:
      - 'okf/**/*.md'
      - 'okf/okf-base.yaml'
      - 'okf-base.yaml'
      - 'index.md'
      - 'domains/**/*.md'
      - 'projects/**/*.md'
      - 'patterns/**/*.md'
      - 'agents/**/*.md'
      - 'glossary.md'

jobs:
  lint:
    uses: nebarilabs/.github/.github/workflows/okflint.yml@v2
    with:
      invariants: true
