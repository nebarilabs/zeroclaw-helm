# Centrally managed by nebarilabs/.github — do not edit in this repo.
# The org sync (nebarilabs/.github .github/workflows/sync.yml) overwrites this
# file directly; downstream edits are silently lost. Propose changes upstream.
name: OpenCode Issue Analysis
on:
  issue_comment:
    types: [created]
jobs:
  analyze:
    if: github.event.issue.pull_request == null && contains(github.event.comment.body, '/opencode')
    uses: nebarilabs/.github/.github/workflows/opencode-issue.yml@v2
    with:
      issue-number: ${{ github.event.issue.number }}
    secrets: inherit
